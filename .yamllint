# .yamllint — sensible defaults for GitHub Actions workflows
extends: default
rules:
  truthy:
    allowed-values: ['true', 'false', 'on', 'off']
  line-length:
    max: 120
  document-start: disable
