[project]
name = "sentinel"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.43.15",
    "fastapi>=0.139.0",
    "uvicorn[standard] >= 0.48.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "moto>=5.2.1",
    "pytest>=9.0.3",
]
