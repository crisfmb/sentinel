[project]
name = "sentinel"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.43.62",
    "fastapi>=0.136.3",
    "uvicorn[standard] >= 0.48.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "moto>=5.2.2",
    "pytest>=9.1.0",
]
